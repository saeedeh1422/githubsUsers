<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.GithubUsers" parent="Theme.AppCompat.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/toolbar_color</item>
        <item name="colorPrimaryVariant">@color/toolbar_color</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="android:windowBackground">@color/background</item>

        <!-- Secondary brand color. -->
        <!-- Status bar color. -->
        <!-- Customize your theme here. -->
    </style>

    <style name="TextGray3">
        <item name="android:textColor">@color/gray3</item>
    </style>

    <style name="TextWhiteXXLarge">
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">@dimen/textXXLarge</item>
    </style>
    <style name="TextWhiteTitle">
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">@dimen/textLarge</item>
    </style>
    <style name="TextWhiteBody">
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">@dimen/textNormal</item>

    </style>
    <style name="TextWhiteDesc">
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">@dimen/textSmall</item>
    </style>
</resources>